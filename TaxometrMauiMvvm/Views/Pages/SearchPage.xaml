<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TaxometrMauiMvvm.Views.Pages.SearchPage"
             xmlns:models="clr-namespace:TaxometrMauiMvvm.Models.Pages"
             xmlns:cells="clr-namespace:TaxometrMauiMvvm.Models.Cells"
             x:DataType="models:SearchViewModel"
             Shell.NavBarIsVisible="False">
    <StackLayout
        Padding="10, 25, 10, 10"
        Spacing="25"
        BackgroundColor="{AppThemeBinding Dark={StaticResource BackgroundDarked}, Light={StaticResource BackgroundLighted}}">
        <StackLayout
            Orientation="Horizontal"
            VerticalOptions="Start"
            HorizontalOptions="Fill"
            Margin="14, 0, 14, 0">
            <StackLayout
                Orientation="Horizontal"
                HorizontalOptions="Start"
                Spacing="5">
                <Border
                    HorizontalOptions="Start"
                    WidthRequest="50"
                    HeightRequest="50"
                    Padding="10"
                    BackgroundColor="{AppThemeBinding Dark={StaticResource BackgroundDarkGray}, Light={StaticResource BackgroundLightGray}}"
                    Stroke="{AppThemeBinding Dark={StaticResource BackgroundDarkGray}, Light={StaticResource BackgroundLightGray}}">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="50"/>
                    </Border.StrokeShape>

                    <ImageButton
                        x:Name="FlayoutBtn"
                        Clicked="FlayoutBtn_Clicked">
                        <ImageButton.Source>
                            <FontImageSource
                                FontFamily="IconsSolid"
                                Color="{AppThemeBinding Light={StaticResource TextLight}, Dark={StaticResource TextDark}}"
                                Glyph="{StaticResource IconMenuBars}"/>
                        </ImageButton.Source>
                    </ImageButton>
                </Border>

                <Label
                Style="{StaticResource TitleText}"
                Text="Поиск устройств"/>
            </StackLayout>
        </StackLayout>
        <Border
            Padding="5"
            VerticalOptions="FillAndExpand"
            BackgroundColor="{AppThemeBinding Dark={StaticResource BackgroundDarkGray}, Light={StaticResource BackgroundLightGray}}">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="40"/>
            </Border.StrokeShape>

            <ListView
                ItemsSource="{Binding Devices}"
                ItemTapped="ListView_ItemTapped"
                BackgroundColor="Transparent"
                VerticalOptions="Start"
                HasUnevenRows="True"
                SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="{x:Type cells:SearchedDeviceViewModel}">
                        <ViewCell
                            Height="120">
                            <ViewCell.View>
                                <Grid
                                    BackgroundColor="{AppThemeBinding Dark={StaticResource BackgroundDarkGray}, Light={StaticResource BackgroundLightGray}}"
                                    HeightRequest="120"
                                    Padding="10">
                                    <Border
                                        Margin="-5, 3.5, 5, -3.5"
                                        VerticalOptions="FillAndExpand"
                                        ZIndex="-2"
                                        BackgroundColor="{AppThemeBinding Dark={StaticResource BackgroundDarked}, Light={StaticResource BackgroundLight}}">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="20"/>
                                        </Border.StrokeShape>
                                    </Border>

                                    <Border
                                        VerticalOptions="FillAndExpand"
                                        ZIndex="-1"
                                        BackgroundColor="{AppThemeBinding Dark={StaticResource BackgroundDark}, Light={StaticResource BackgroundLighted}}">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="20"/>
                                        </Border.StrokeShape>
                                        <Grid
                                            Padding="5"
                                            RowDefinitions=".5*, .5*"
                                            RowSpacing="5">
                                            <Label
                                                FontSize="Large"
                                                VerticalTextAlignment="End"
                                                Text="{Binding CustomName}"/>
                                            <Label
                                                Grid.Row="1"
                                                FontSize="Micro"
                                                TextColor="{AppThemeBinding Dark={StaticResource TextColoredDark}, Light={StaticResource TextColoredLight}}"
                                                Text="{Binding ConnectionStateRU}"/>
                                        </Grid>
                                    </Border>

                                </Grid>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>

                <ListView.Footer>
                    <ActivityIndicator
                        IsRunning="True"/>
                </ListView.Footer>
            </ListView>
            
        </Border>
        
    </StackLayout>
</ContentPage>